package leo.shared;

public class BuildConfig {
    // These are options that are currently configurable at build time
    // - mostly because they act both on server and client side.

    public static final boolean skipTutorial = false;
}
