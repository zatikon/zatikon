///////////////////////////////////////////////////////////////////////
//	Name:	ImagePanel
//	Desc:	A panel that displays an image.
//	Date:	2/7/2003 - Gabe Jones
//	TODO:
///////////////////////////////////////////////////////////////////////
package leo.client;

// imports

import java.awt.*;
import java.net.URL;


class ImagePanel extends Panel {

    /////////////////////////////////////////////////////////////////
    // Properties
    /////////////////////////////////////////////////////////////////
    private final Toolkit tk = Toolkit.getDefaultToolkit();
    private final Image image;


    /////////////////////////////////////////////////////////////////
    // Constructor
    /////////////////////////////////////////////////////////////////
    public ImagePanel(String imageName) {
        super();
        URL url = this.getClass().getClassLoader().getResource("art/" + imageName);
        image = tk.createImage(url);
        //image = tk.createImage("./art/" + imageName);
    }


    /////////////////////////////////////////////////////////////////
    // Repaint
    /////////////////////////////////////////////////////////////////
    public void paint(Graphics g) {
        g.drawImage(image, 0, 0, this);
        super.paint(g);
    }
} 
